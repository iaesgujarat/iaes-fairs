// IAES brand assets. The logo file lives in /public so it is served at a
// stable URL — usable from <img>, client-side react-pdf <Image>, and
// (via an absolute URL) transactional emails.
//
// v17 (2026-05-20): switched from the legacy 500px white-background
// JPEG to the transparent emblem at /icons/icon-512.png. Same file
// serves the PWA standard icon (manifest "any" purpose) and every
// app surface (header, footer, admin login, invoice PDFs, emails).

export const IAES_LOGO_PATH = "/icons/icon-512.png";

export const IAES_LOGO_ALT = "IAES — Indo American Education Society";
